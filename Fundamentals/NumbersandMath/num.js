const num1 = 100;
const num2 = 50;

let val;

//Simple math with numbers
val = num1+num2
val = num1-num2
val = num1/num2
val = num1*num2
val = num1%num2

//math object
val = Math.PI;
val = Math.E
val = Math.round(2.4) //en yakın olana
val = Math.ceil(2.4) //yukarıya
val = Math.sqrt(64)
val = Math.abs(-3)
val = Math.pow(2,3)
val = Math.min(2,4,545,7)
val = Math.max(2,4,545,7)
val = Math.random()
val = Math.floor(Math.random()*15)


console.log(val)